const state = {
  contact: {
    number: 998332001070,
    title: '+998(33) 200-10-70'
  }
};

const getters = {
  contact(state) {
    return state.contact
  }
};

const mutations = {
};

const actions = {
};

export default {state, getters, mutations, actions, namespaced: true};